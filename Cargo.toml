[package]
name = "hyperpack"
version = "0.3.0"
edition = "2024"
description = "Exact-aware packing carriers and feasibility replay for the Hyper ecosystem"
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/timschmidt/hyperpack"
keywords = ["packing", "optimization", "geometry", "hyperreal", "cad"]
categories = ["algorithms", "mathematics", "science"]

[features]
default = []
dispatch-trace = ["hyperreal/dispatch-trace"]

[dependencies]
# Cargo uses these paths in the workspace and retains the version requirements
# in the published manifest.
hypercurve = { version = "0.3.1", path = "../hypercurve" }
hyperreal = { version = "0.13.1", path = "../hyperreal", features = ["serde"] }

# Keep transitive sibling crates unified while using local path development mode.
[patch.crates-io]
hyperlattice = { path = "../hyperlattice" }
hyperlimit = { path = "../hyperlimit" }
hyperreal = { path = "../hyperreal" }
hypersolve = { path = "../hypersolve" }

[dev-dependencies]
proptest = "1.7.0"

[[bench]]
name = "feasibility"
harness = false

[[bench]]
name = "replay_micro"
harness = false
